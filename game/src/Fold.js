import Point from './models/Point';
import Line from './models/Line';
import GameState from './models/GameState';
import GameCanvas from './GameCanvas';

/*This function will not return anything, but will directly add line(s)/points to the
state in GameCanvas */
/*Input two points that the user clicks on the paper, this method handles adding all
**lines and points created by folding the paper on the line created between the 2 points */
function initFold(pt1, pt2){
    /*
    code (I removed the pseudo code for now, I'll implement it later)
    */
    return new Point(400, 200)
}
